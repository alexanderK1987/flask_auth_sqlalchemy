# Flask Application Template with JWT Authentication
As title. 
Implementing with SQLAlchemy.

## Run the app
`run: flask run`

## Initialize Database
db initialization:
`flask db init`

db migration:
`flask db migrate -m "initial commit"`

db upgrate:
`flask db upgrade`
